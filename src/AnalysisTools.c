/*
 * AnalysisTools.c — intentionally empty.
 *
 * Functions previously defined here have been moved to:
 *   Coordinates.c  — coordinate/geometry operations (RestorePBC, Distance,
 *                     CentreOfMass, GeomCentre, WrapJoinCoordinates, Gyration)
 *   System.c       — system lookup helpers (FindBeadType, FindMoleculeName,
 *                     FindMoleculeType)
 *   ReadWrite.c    — file-type detection (FileType, FileTypeFromString,
 *                     InputCoorStruct, StructureFileType, CoordinateFileType)
 *   Aggregates.c   — aggregate construction (EvaluateContacts,
 *                     RemovePBCAggregates)
 *   MainLoop.c     — step selection (UseStep)
 *   Debug.c        — verbose/debug output (VerboseOutput, PrintCount, etc.)
 */
